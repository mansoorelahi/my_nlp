%table.table
  %thead
    %tr
      %th #
      %th location
      %th name
      %th money
    %tbody
      - data.each_with_index do |entry, index|
        %tr
          %td
            = index + 1
          %td
            = entry
